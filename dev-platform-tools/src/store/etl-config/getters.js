// 导航列表
export const meunInfo = state => state.meunInfo;
// 横向导航菜单信息
export const oneMenuList = state => state.oneMenuList;
// 侧边栏导航信息
export const otherMenuList = state => state.otherMenuList;
// 侧边栏是否缩放
export const isShrink = state => state.isShrink;
export const width = state => state.width;